# SandWorkflow Workflow 迁移进度

当前开发版本：`1.0.7`

## 当前 FLOW 基线（2026-08-20）

| 关口 | 当前状态 | 可复核证据 |
| --- | --- | --- |
| 需求 / 架构 / 票据 | 已检查 | 权威来源、宿主边界、PostgreSQL 约束和核心流程已记录。 |
| 模块实现 | 已验证部分 | 安装菜单引用的 11 个前端组件在权威包中全部存在；包、后端、前端已与 `sandadmin-demo-host` 验收宿主逐文件比对一致。 |
| 正式 FLOW 验收 | 未通过 | 安装菜单引用的 11/11 个组件均已在本机宿主实际加载；列表接口可读取空数据，但尚未完成正式业务用例清单。 |
| 本地业务闭环 | 未通过 | 尚未完成“建分组 → 建流程 → 发布 → 发起 → 审批 → 查询”的有数据闭环。 |
| 可上线部署 | 未通过 | 尚未完成干净 PostgreSQL 安装、真实版本升级、卸载清理和卸载后宿主复验。 |

2026-08-20 复核发现，本机宿主曾保留 SandWorkflow 菜单数据但缺少发布包、后端和前端载荷，导致所有菜单组件加载失败。恢复完整载荷并 reload 后，组件缺失已消除；这只证明宿主同步和空页面可访问，不等于业务闭环或发布验收。

## 已完成

- SandWorkflow Workflow 插件目录、安装 SQL、菜单与权限。
- 流程分组、流程定义、发起、待办、已处理、实例数据页面。
- 顺序审批、会签、或签可视化编辑入口及高级 JSON 兼容模式。
- 条件分支运行时计算与可视化条件分流设计器。
- 指定人员、发起人本人、角色审批人解析。
- 会签、或签及逐人处理记录。
- 任务处理人明细表与索引化待办查询。
- 发布前节点、分支、条件和处理人规则校验。
- 审批与撤销统一锁顺序，避免反向加锁。
- 实例流程定义快照。
- 不可变流程定义版本表、实例版本关联及版本历史 UI。
- 发起和审批请求幂等键。
- 依次审批的处理人顺序、越级接口拦截和逐人激活待办。
- 待办、已办、我的流程和实例数据统一使用流程详情抽屉。
- 0.3/0.4 数据升级兼容、待办处理人回填和依次审批序号迁移。

## 已纳入主线的后端优化

- 将节点解析、条件计算、处理人解析逐步拆分为独立服务。
- 使用 Outbox 保存待办、通知和流程完成事件，事务提交后异步投递。
- 为发布、发起、审批、撤销补充审计上下文和稳定错误码。
- 增加单元测试、数据库集成测试与权限测试。

## demo 验收门槛

- 全新安装和 0.3/0.4 升级 SQL 均成功。
- 菜单、路由、权限码和接口前缀一致。
- 顺序审批、条件分支、会签、或签、拒绝和撤销闭环通过。
- 重新发布定义不影响运行中的旧版本实例。
- 重复发起和重复审批不产生重复业务数据。
- PHP 语法检查、前端类型检查和生产构建通过。
- 并发审批与撤销无重复流转、无明显死锁。
